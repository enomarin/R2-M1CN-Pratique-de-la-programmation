// 1° créer une variable position
//PVector pos;
// 2° Créer une variable déplacement initialisé avec une valeur de 5
//float deplacement;
void setup() {
  size(400, 400);
  // 3° Initialiser la variable avec une position aléatoire dans le canvas
  //pos = new PVector(width/2, height/2);
  background(10);
}

void draw() {
  background(10, 10);
  noStroke();
  //fill(255,10)
  // 4° Dessiner le cercle a la position x et y du vecteur:;

// 5° Decommenter la fonction suivante pour générer un nombre entier aléatoire entre 0 et 3
  //int direction = floor(random(4));
  //println(direction);

//// 6° Pour chacune des valeur de direction, déplacer le vecteur de la valeur du déplacement
  /*
  if (direction == 0) {
    pos.y = pos.y + deplacement; // Exemple pour pos.y
  }
  if (direction == 1) {
  }
  if (direction == 2) {
  }
  if (direction == 3) {
  }
  */
}
